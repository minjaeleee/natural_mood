import styles from './MoreCartInfo.module.scss'

export const MoreCartInfo = () => {
  return (
    <div className={styles.wrapper}>
      <div className={styles.innerWrapper}>
        innerWrapper
      </div>
    </div>
  )
}
